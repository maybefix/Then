# v0.6.0 ページ編集 改善実装 進捗メモ

最終更新: 2026-08-28（作業中断時点）  
ブランチ: `codex/v0.6.0-features`  
対象バグと方針: `V0.6.0_PAGING_AUDIT_AND_FIX_PROPOSAL.md`

## 現在の状態

型チェック（`npx tsc --noEmit`）は通っている。`npm run build` と回帰テスト一式は**未実行**。実画面検証はバグ1・2が合格、バグ3が修正途中（下記）。

変更ファイルは2つのみ:
- `src/VerticalTextEditor.tsx`（+約230行）
- `src/App.tsx`（+9行、`textLayoutSignature` を渡すだけ）

## 実装済みと検証結果

### 案1: 内容アンカーによる表示位置復元（バグ1）— 実装済み・検証合格

- `pagedAnchorPosRef` に「現在ページ先頭の文書位置」を記録。記録は `requestPagedScrollSettle` の安定確定時（`capturePagedAnchor`）。
- `syncPageMetrics` のレイアウト差分検出ブロックから `requestPagedAnchorRestore` を呼ぶ。初回計測（`DEFAULT_PAGE_LAYOUT` からの遷移）は除外。
- `requestPagedAnchorRestore` はアンカーが属するページへ、目標ページと実スクロール位置が一致するまで最大12フレーム追従する。
- 復元スクロール中に `capturePagedAnchor` がアンカーを上書きしないようガード済み。
- **検証**: 縦書き縦送りで10ページ目（第045段落）表示中に1280×720→900×560へ縮小 → 第045段落を含む45/176ページへ復元。白紙ページ（ホストとビューポートの重なり0）も解消。1100×720へ戻す往復でもアンカー段落が表示内に維持された。

### 案2: 文字設定変更でのページ再同期（バグ2）— 実装済み・検証合格

- `VerticalTextEditorProps` に `textLayoutSignature: string` を追加。App側でフォント・文字サイズ・行間・文字表示幅（縦横）を `|` 連結して渡す。
- 同期エフェクトを2つに分割。`text` のみのものと、`editorDisplayMode` / `pageFlowDirection` / `writingMode` / `textLayoutSignature` で復元まで行うもの。初回マウントは同期のみ（初期ビューポート復元との競合回避）。
- `handleFontLoadingDone` に `requestPagedAnchorRestore` を追加（Webフォント読込完了もカバー）。
- **検証**: 文字サイズ15→21pxで総ページが即座に45へ更新（実測期待値45と一致）。アンカー段落（第040段落）も表示内に維持。修正前は14ページのまま後半ページへ到達不能だった。

### 案4（軽微）— 実装済み・未検証

- 改行記号レイヤー: ページ表示時はスクロール領域（余白24px込み）外のマークを描画しない。
- ホイールのページ送りを `deltaY` のみに限定（RTLでのdeltaX符号問題とチルトホイール誤爆の回避）。

## 中断地点：案3（バグ3・IME変換中のページ追従）

**状況**: 実装は書き終えたが、最後の修正を入れた直後で**未検証**。

経緯:
1. 最初の実装は `editor.state.selection.head` の座標を使ったが、composition中はProseMirrorのselectionがDOM実キャレットより遅れるため機能しなかった（ページは動くがキャレットは画面外のまま）。
2. DOM選択（`window.getSelection()` の range矩形）から直接座標を読むよう変更。
3. それでも1回のイベントで1ページ分しか進まないことを実測で確認（1回目 7/31でキャレットtop=760・非表示 → 2回目のcompositionupdateで 8/31・top=133・表示）。断片位置の再正規化が1フレーム1ページ分しか進まないため。
4. **直前に入れた修正（未検証）**: `handleCompositionUpdate` の追従処理を、キャレットが表示域に入るまで最大8フレーム繰り返すループに変更した。

**次にやること**: この8フレームループが1回のcompositionupdateで収束するかを実画面で確認する。手順は下の「検証ハーネス」を使い、6ページ目の最終段落末尾にキャレットを置いてcompositionstart→長い読み仮名挿入→compositionupdateを1回だけ発火し、`window.__caret().visible === true` になるかを見る。

## 残作業

1. 案3の検証（上記）
2. 横書き（horizontal-tb）・横送りでの案1・案2の再確認（今回は縦書き縦送り中心に検証した）
3. `npm run build`
4. 回帰テスト: `test:v058-high-risk`、`test:v059-visibility`、`test:v0510-ime-visual-lines`、`test:performance`
5. マトリクス（`V0.6.0_PAGING_TEST_MATRIX.md`）の該当行を「修正済み・確認済み」へ更新
6. 実機確認が必要な項目（提案書末尾に列挙）: Windows実IME、実スクロールバードラッグ、smoothスクロール中の連打、高DPI、トラックパッド横スワイプ
7. 問題なければ `npm run tauri:build` でインストーラー再生成（前回SHA `2DF072EE...` は失効扱いになる）

## 検証ハーネス（ブラウザペインが非表示のとき必須）

`document.hidden === true` だと rAF・scrollイベント・smoothスクロールが止まり、偽バグを観測する。エディタ画面へ入った後に以下を流し込む。

```js
// rAFをsetTimeoutで代替、scrollToをauto化してscrollイベントを同期発火
if (!window.__rafShimmed) {
  window.__rafShimmed = true; window.__rafMap = new Map(); let nextId = 1e9;
  const origCancel = window.cancelAnimationFrame.bind(window);
  window.requestAnimationFrame = (cb) => { const id = nextId++;
    window.__rafMap.set(id, setTimeout(() => { window.__rafMap.delete(id); cb(performance.now()); }, 16)); return id; };
  window.cancelAnimationFrame = (id) => { if (window.__rafMap.has(id)) { clearTimeout(window.__rafMap.get(id)); window.__rafMap.delete(id); } else origCancel(id); };
}
if (!window.__scrollPatched) {
  window.__scrollPatched = true; const orig = Element.prototype.scrollTo;
  Element.prototype.scrollTo = function(opts) {
    if (opts && typeof opts === 'object') orig.call(this, {...opts, behavior: 'auto'}); else orig.apply(this, arguments);
    this.dispatchEvent(new Event('scroll')); };
}
```

状態観測用のヘルパー（`__state()` / `__caret()` / `__paste()`）はこのセッションで使ったものを再定義すればよい。要点は、`.verticalTypewriterScroller` の scrollTop/scrollLeft、`--paged-host-y` / `--paged-fragment-y`、`.pagedEditorSheet` の枚数、ホストとビューポートの重なり、`.visibleLineNumberLayer` の行番号を同時に見ること。

ブラウザモードでは起動時ポータルの「新しい原稿を始める」を押してエディタへ入り、本文は `ClipboardEvent('paste')` で流し込む（localStorage の `then.app-state.v1` の `markdown` は browser ランタイムでは復元されない）。設定は同じキーの `settings` を書き換えてリロードするか、設定モーダルのselect/inputを直接操作する。

## 注意

- テスト用に localStorage の設定を書き換えた場合、`editorDisplayMode` / `pageFlowDirection` / `showLineNumbers` / `fontSize` / `uiFontScale` を元へ戻すこと。
- 現時点で `settings.editorDisplayMode = "paged"`、`pageFlowDirection = "vertical"`、`showLineNumbers = true`、`fontSize = 15` が入ったままの可能性がある。
